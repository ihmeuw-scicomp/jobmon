"""Routes used to move through the finite state."""

from jobmon.server.web.routes.v2.fsm import (array,
                                             cluster,
                                             dag,
                                             node,
                                             queue,
                                             task,
                                             task_instance,
                                             task_template,
                                             task_resources,
                                             tool,
                                             tool_version,
                                             workflow,
                                             workflow_run)
